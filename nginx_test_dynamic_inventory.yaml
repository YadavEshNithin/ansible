- name: nginx install
  hosts: dynamic_frontend
  become: yes
  tasks:
  - name: install the nginx 
    ansible.builtin.dnf:
      state: present
  
  - name: run or start the nginx
    ansible.builtin.service:
      state: started
      enabled: yes